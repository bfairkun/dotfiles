config/.config/nnn/sessions/
config/.config/gh/hosts.yml
**/__pycache__/

# Secret credentials — never commit
zsh/.secrets
.secrets

# Per-repository agent approvals
.claude/settings.local.json

# Claude app skills synced from the cloud (runtime state)
agents/.agents/skills/synced/

# Editor/manual backup copies
*.bak

# macOS
.DS_Store
