{
	"R Script Template": {
		"prefix": "rscripttemplate",
		"body": [
			"#!/usr/bin/env Rscript",
			"",
			"######################################################################",
			"# @author      : ${1:Your Name} (${2:your@email.com})",
			"# @file        : $TM_FILENAME",
			"# @created     : $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE $CURRENT_HOUR:$CURRENT_MINUTE",
			"#",
			"# @description : $0",
			"######################################################################",
			"",
			"# Use hard coded arguments in interactive R session, else use command line args",
			"if(interactive()){",
			"    args <- scan(text=",
			"                 \"InteractiveMode Test Args\", what='character')",
			"} else{",
			"    args <- commandArgs(trailingOnly=TRUE)",
			"}",
			""
		],
		"description": "Insert R script template with author, file, and argument handling"
	}
}