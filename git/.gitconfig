[user]
	email = bfair.kun@gmail.com
	name = Benjmain Fair
	signingkey = C5065CAB42BDF2C5
[core]
	editor = vim
	excludesfile = /home/bjf79/.gitignore_global
[merge]
	tool = vimdiff
	conflictstyle = diff3
[mergetool]
	prompt = false
	keepBackup = false
[alias]
      gr = log --graph --full-history --all --color --pretty=tformat:"%x1b[31m%h%x09%x1b[32m%d%x1b[0m%x20%s%x20%x1b[33m(%an)%x1b[0m""]]]]]"
[credential]
	helper = cache --timeout 3600
[alias]
    co  = checkout       # Checkout a branch
    cob = checkout -b    # Checkout a new not yet existing branch
    f   = fetch -p       # Fetch from a repository and prune any remote-tracking references that no longer exist on the remote.
    up = !git pull --rebase --prune $@ && git submodule update --init --recursive
    ba  = branch -a      # List both remote-tracking branches and local branches.
    st = status -sb
    gr = log --graph --full-history --all --color --pretty=tformat:"%x1b[31m%h%x09%x1b[32m%d%x1b[0m%x20%s%x20%x1b[33m(%an)%x1b[0m"
[commit]
	gpgsign = false
[oh-my-zsh]
	hide-dirty = 1
	hide-status = 1
[include]
    path = ~/.gitconfig_local
