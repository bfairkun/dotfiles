\section{Introduction}

% Write however feels natural -- wrap lines wherever, don't think about it.
% `make tidy` reflows the prose to one sentence per line before you commit,
% so git diffs show which sentence changed instead of a reflowed blob.
%
% A blank line starts a new paragraph. That is the only paragraph syntax.
% A single newline is invisible to LaTeX.

\todo{introduction}
